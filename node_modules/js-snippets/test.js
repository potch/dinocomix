var s = require('./snippets');

console.log('slice');

function sliceTest() {
  return s.slice(arguments, 1);
}

console.log(sliceTest(1,2,3,4));

console.log('get');

function get(p, o) {
  return o[p];
}

var getX = s.partial(get, 'x');

console.log(getX({x: {y:1}}));

console.log('queryParams');

console.dir(s.queryParams('http://foo.biz/?bar=1&foo'));
